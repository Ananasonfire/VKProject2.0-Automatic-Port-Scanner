## ПОЛНОЕ РУКОВОДСТВО ПО УСТАНОВКЕ И ЗАПУСКУ

### Требования

- **ОС:** Linux (Ubuntu 20+, Debian 10+), macOS, или Windows (WSL2)
- **Python:** 3.11 или выше
- **権限:** Sudo/Admin для запуска Masscan
- **Интернет:** Для получения баннеров и CVE проверок

### ШАГИ УСТАНОВКИ

#### 1️⃣ Установка системных зависимостей

**Ubuntu/Debian:**
```bash
sudo apt update
sudo apt install -y masscan nmap python3-pip python3-dev build-essential
```

**macOS:**
```bash
brew install masscan nmap
```

**Windows (WSL2):**
```bash
# В WSL терминале
sudo apt update
sudo apt install -y masscan nmap python3-pip
```

#### 2️⃣ Клонирование проекта

```bash
# Создать директорию проекта
mkdir -p ~/projects/port-scanner
cd ~/projects/port-scanner

# Скопировать все файлы проекта в эту папку
# (main.py, config.py, scanner.py, notify.py, storage.py, dashboard.py, 
#  banner_analyzer.py, cve_checker.py, models.py, utils.py, config.yaml)
```

#### 3️⃣ Создание виртуального окружения

```bash
# Создать виртуальное окружение
python3 -m venv venv

# Активировать (Linux/macOS)
source venv/bin/activate

# ИЛИ активировать (Windows/PowerShell)
venv\Scripts\Activate.ps1
```

#### 4️⃣ Установка зависимостей Python

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

Все зависимости:
- pyyaml (парсинг конфигурации)
- pydantic (типизация данных)
- aiofiles (асинхронные файлы)
- aiohttp (асинхронные HTTP запросы)
- httpx (синхронные HTTP запросы)
- requests (HTTP запросы)
- APScheduler (периодическое выполнение)
- flask (веб-дашборд)

#### 5️⃣ Настройка конфигурации

**Отредактировать config.yaml:**

```bash
nano config.yaml  # или используйте ваш редактор
```

**Минимальная конфигурация:**

```yaml
scan:
  targets: ["192.168.1.0/24"]  # Ваша сеть
  ports: [22, 80, 443]

notifications:
  telegram:
    enabled: false  # Или true если хотите уведомления
    token: "YOUR_BOT_TOKEN"
    chat_id: "YOUR_CHAT_ID"
```

#### 6️⃣ Тестирование установки

```bash
# Проверить Masscan
masscan --version

# Проверить Python зависимости
python -c "import pydantic, pyyaml, aiohttp; print('All OK')"

# Проверить конфигурацию
python main.py --help
```

---

### ЗАПУСК ПРИЛОЖЕНИЯ

#### Вариант 1: Одно сканирование

```bash
# Запустить одно сканирование и выйти
python main.py --once
```

**Вывод:**
```
[INFO] STARTING SCAN SESSION
[INFO] Запуск Masscan для целей: ['192.168.1.0/24']
[INFO] Сканирование завершено за 45 сек
[INFO] Найдено 12 открытых портов
[INFO] Отправлено 12 уведомлений
```

#### Вариант 2: Периодическое сканирование

```bash
# Запустить с планировщиком (фоновый режим)
python main.py --scheduler

# ИЛИ в экране для долгого запуска
screen -S scanner python main.py --scheduler

# Отсоединиться: Ctrl+A затем D
# Переподключиться: screen -r scanner
```

#### Вариант 3: Веб-дашборд

```bash
# В отдельном терминале (с активированным venv)
python dashboard.py

# Перейти в браузер
# http://127.0.0.1:5000
```

**Функции дашборда:**
- 📊 Статистика в реальном времени
- 📋 История всех сканирований
- 🎯 Список открытых портов
- ▶️ Кнопка для запуска сканирования
- 🔄 Автоматическое обновление

#### Вариант 4: С отладкой

```bash
# Расширенное логирование
python main.py --once --debug

# Все логи записываются в scan.log
tail -f scan.log
```

---

### НАСТРОЙКА УВЕДОМЛЕНИЙ

#### 🤖 Telegram Bot

**Шаг 1: Создать бота**
1. Откроть Telegram и найти [@BotFather](https://t.me/botfather)
2. Отправить `/newbot`
3. Введить имя (например, "PortScannerBot")
4. Скопировать полученный TOKEN

**Шаг 2: Получить Chat ID**
1. Добавить бота в группу или личный чат
2. Отправить сообщение боту
3. Открыть: https://api.telegram.org/bot<TOKEN>/getUpdates
4. Найти `"chat"{"id": XXXX}` - это Chat ID

**Шаг 3: Обновить config.yaml**
```yaml
notifications:
  telegram:
    enabled: true
    token: "123456789:ABCdefGHIjklmnoPQRstUVwxyz"
    chat_id: "987654321"
```

#### 📧 Email (Gmail)

**Шаг 1: Включить 2FA в Google аккаунте**
- Перейти на https://myaccount.google.com
- Security → 2-Step Verification → Enable
- Создать App Password

**Шаг 2: Обновить config.yaml**
```yaml
notifications:
  email:
    enabled: true
    smtp_server: "smtp.gmail.com"
    smtp_port: 587
    sender_email: "your_email@gmail.com"
    sender_password: "xxxx xxxx xxxx xxxx"  # App password
    recipient: "recipient@gmail.com"
```

#### 🎮 Discord Webhook

**Шаг 1: Создать Webhook**
1. Правый клик на канал → Edit Channel → Integrations → Webhooks
2. Create Webhook → Copy Webhook URL

**Шаг 2: Обновить config.yaml**
```yaml
notifications:
  discord:
    enabled: true
    webhook_url: "https://discord.com/api/webhooks/..."
```

---

### ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ

#### 📍 Сканирование локальной сети

```yaml
scan:
  targets:
    - "192.168.1.0/24"
    - "192.168.2.0/24"
  rate: 5000
  timeout: 120
```

#### 🌐 Сканирование нескольких хостов

```yaml
scan:
  targets:
    - "8.8.8.8"
    - "1.1.1.1"
    - "208.67.222.222"
  rate: 100
  timeout: 300
```

#### ⏰ Ежедневное сканирование в определённое время

```yaml
schedule:
  enabled: true
  cron: "0 2 * * *"  # 2:00 AM каждый день
```

#### 🔄 Сканирование каждые 30 минут

```yaml
schedule:
  enabled: true
  cron: "*/30 * * * *"  # Каждые 30 минут
```

#### 🌙 Сканирование в выходные дни

```yaml
schedule:
  enabled: true
  cron: "0 0 * * 0,6"  # Полночь в субботу и воскресенье
```

---

### ЧАСТЫЕ ПРОБЛЕМЫ И РЕШЕНИЯ

#### ❌ "masscan: command not found"

```bash
# Linux
sudo apt install masscan

# macOS
brew install masscan

# Проверить
which masscan
masscan --version
```

#### ❌ "Permission denied" при запуске

```bash
# Masscan требует привилегий
sudo python main.py --once

# ИЛИ добавить sudo для Masscan
sudo setcap cap_net_raw=ep /usr/bin/masscan
```

#### ❌ "timeout" при получении баннеров

- Увеличить timeout в config.yaml
- Уменьшить количество портов
- Проверить сетевое соединение

#### ❌ Уведомления не отправляются

1. Проверить интернет
2. Проверить token/chat_id (без пробелов)
3. Включить debug:
   ```bash
   python main.py --once --debug
   ```
4. Проверить логи:
   ```bash
   tail -f scan.log | grep -i telegram
   ```

#### ❌ "Module not found" ошибки

```bash
# Переустановить зависимости
pip install --force-reinstall -r requirements.txt

# Или обновить pip
pip install --upgrade pip
```

---

### ПРОДВИНУТЫЕ ОПЦИИ

#### 🔍 CVE проверки (опционально)

Нужен API ключ от [vulners.com](https://vulners.com):

```yaml
cve_check:
  enabled: true
  api_key: "YOUR_API_KEY"
```

Тогда при обнаружении сервиса будут автоматически проверяться известные CVE.

#### 🎨 Кастомизация дашборда

Отредактировать HTML в dashboard.py:
- Изменить цвета (переменные CSS)
- Добавить новые метрики
- Изменить интервал обновления

#### 📊 Экспорт результатов

```bash
# Экспортировать в JSON
python -c "
from storage import create_storage
import json
storage = create_storage('sqlite', 'scan_history.db')
results = storage.get_all_results()
with open('export.json', 'w') as f:
    json.dump([r.dict() for r in results], f, indent=2, default=str)
"
```

#### 🔐 Запуск с контролем прав

```bash
# Создать отдельного пользователя
sudo useradd -r -s /bin/false scanner

# Дать права на Masscan
sudo setcap cap_net_raw=ep /usr/bin/masscan

# Запустить от пользователя
sudo -u scanner python main.py --scheduler
```

---

### МОНИТОРИНГ ЛОГОВ

```bash
# Смотреть логи в реальном времени
tail -f scan.log

# Фильтр по типу
grep ERROR scan.log
grep "Telegram" scan.log

# Последние 50 строк
tail -50 scan.log

# Поиск по IP
grep "192.168.1.5" scan.log
```

---

### ОСТАНОВКА И ПЕРЕЗАГРУЗКА

```bash
# Если запущено в foreground: Ctrl+C

# Если в screen:
screen -r scanner
# Затем: Ctrl+C

# Если в фоне с PID:
ps aux | grep main.py
kill <PID>
```

---

### ПОЛУЧЕНИЕ ПОМОЩИ

1. Проверить логи: `tail -f scan.log`
2. Включить debug: `--debug` флаг
3. Проверить config.yaml синтаксис (должен быть валидный YAML)
4. Убедиться что Masscan установлен: `masscan --version`
5. Проверить интернет для уведомлений

---

## БЫСТРЫЙ СТАРТ (5 МИНУТ)

```bash
# 1. Установить зависимости
sudo apt install -y masscan python3-pip
pip install -r requirements.txt

# 2. Настроить config.yaml (минимум)
# Отредактировать targets и включить/выключить уведомления

# 3. Запустить один раз
python main.py --once

# 4. Если всё работает - запустить с расписанием
python main.py --scheduler &

# 5. Открыть дашборд в браузере (опционально)
python dashboard.py
# http://127.0.0.1:5000
```

Готово! 🚀
