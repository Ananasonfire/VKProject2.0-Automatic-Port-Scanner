## БЫСТРЫЙ СТАРТ ⚡

### За 5 минут до первого сканирования

#### Шаг 1: Установить Masscan (2 минуты)

**Linux:**
```bash
sudo apt update && sudo apt install -y masscan
```

**macOS:**
```bash
brew install masscan
```

Проверить:
```bash
masscan --version
```

#### Шаг 2: Установить Python зависимости (1 минута)

```bash
python3 -m venv venv
source venv/bin/activate  # Linux/macOS
pip install -r requirements.txt
```

#### Шаг 3: Отредактировать config.yaml (1 минута)

**Минимальный конфиг:**

```yaml
scan:
  targets: ["192.168.1.0/24"]  # ← Измените на вашу сеть
  ports: [22, 80, 443]
  rate: 1000

database:
  type: "sqlite"
  path: "scan_history.db"

notifications:
  telegram:
    enabled: false  # Пока отключить
```

#### Шаг 4: Запустить сканирование (1 минута)

```bash
python main.py --once
```

**Готово! 🎉**

---

### Вывод:

```
[INFO] STARTING SCAN SESSION
[INFO] Сканирование: 192.168.1.0/24
[INFO] Портов для сканирования: 3
[INFO] Сканирование завершено за 12 сек
[INFO] Найдено открытых портов: 5
```

---

### Результат

Результаты сохранены в `scan_history.db`.

Для просмотра веб-интерфейса (в другом терминале):

```bash
python dashboard.py
# Открыть http://127.0.0.1:5000 в браузере
```

---

### Как включить уведомления?

#### 🤖 Telegram (1 минута)

1. Чат с [@BotFather](https://t.me/botfather) → `/newbot`
2. Скопировать Token
3. Отредактировать config.yaml:

```yaml
notifications:
  telegram:
    enabled: true
    token: "123456789:ABCdefGHIjkl"
    chat_id: "987654321"
```

4. Перезапустить:

```bash
python main.py --once
```

Будет сообщение в Telegram! 📬

---

### Как включить периодичность?

**Каждые 4 часа:**

```yaml
schedule:
  enabled: true
  cron: "0 */4 * * *"
```

**Запустить:**

```bash
python main.py --scheduler
```

Будет работать в фоне 🔄

---

### Где результаты?

```bash
# Логи
tail -f scan.log

# Дашборд (браузер)
python dashboard.py  # http://127.0.0.1:5000

# БД (командная строка)
sqlite3 scan_history.db
> SELECT COUNT(*) FROM scan_results;
```

---

### Далее?

- 📖 Читать `INSTALLATION.md` для полной инструкции
- 🏗️ Читать `ARCHITECTURE.md` для деталей кода
- 🔧 Изменить параметры в `config.yaml`
- 💡 Экспериментировать и расширять

---

**Поздравляем! Вы запустили профессиональный сканер портов!** 🚀
